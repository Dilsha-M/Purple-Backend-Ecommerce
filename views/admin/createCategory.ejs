
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create Category</title>
    <link rel="stylesheet" href="/css/style.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: Arial, sans-serif;
        }

        body {
            display: flex;
            min-height: 100vh;
            background-color: #f4f6f7;
            color: #333;
        }

      
        .sidebar {
            width: 250px;
            background-color: #0f5132;
            padding: 20px;
            color: #fff;
            height: 100vh;
            position: fixed;
            top: 0;
            left: 0;
            overflow-y: auto;
        }

        .sidebar h1 {
            color: #ffffff;
            font-size: 1.8em;
            margin-bottom: 20px;
        }

        .sidebar .actions a {
            display: block;
            padding: 10px;
            margin: 10px 0;
            color: #fff;
            text-decoration: none;
            background-color: #1c7430;
            border-radius: 5px;
            transition: background-color 0.3s ease;
        }

        .sidebar .actions a:hover {
            background-color: #154c24;
        }

     
        .dashboard-content {
            margin-left: 270px;
            padding: 20px;
            flex-grow: 1;
        }

        .form-container {
            background-color: #ffffff;
            padding: 20px;
            border-radius: 10px;
            border: 2px solid #0f5132;
            box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
            max-width: 500px;
            margin: 0 auto;
        }

        h2 {
            color: #0f5132;
            font-size: 1.6em;
            margin-bottom: 20px;
            text-align: center;
        }

        label {
            font-size: 1.1em;
            margin-bottom: 8px;
            display: block;
            color: #0f5132;
        }

        input[type="text"] {
            width: 100%;
            padding: 10px;
            margin-bottom: 15px;
            border-radius: 5px;
            border: 2px solid #0f5132;
            font-size: 1em;
        }

        button {
            width: 100%;
            padding: 12px;
            background-color: #1c7430;
            border: none;
            color: #fff;
            font-size: 1.2em;
            cursor: pointer;
            border-radius: 5px;
            transition: background-color 0.3s ease;
        }

        button:hover {
            background-color: #154c24;
        }

        .error-message {
            color: red;
            font-size: 0.9em;
            display: none;
        }

    </style>
</head>

<body>


    <div class="sidebar">
        <h1>Admin Dashboard</h1>
        <div class="actions">
            <a href="/admin/create">Create New Product</a>
            <a href="/admin/products">View All Products</a>
            <a href="/admin/create-category">Create New Category</a>
            <a href="/admin/categories">View All Categories</a>
            <a href="/admin/create-subcategory">Create New Subcategory</a>
            <a href="/admin/logout">Logout</a>
        </div>
    </div>


    <div class="dashboard-content">
        <div class="form-container">
            <h2>Create Category</h2>
            <form action="/admin/create-category" method="POST" onsubmit="return validateForm()">
                <label for="name">Category Name</label>
                <input type="text" name="name" id="name" required>
                <span id="name-error" class="error-message">Category name is required</span>
                <br>

                <button type="submit">Create Category</button>
            </form>
        </div>
    </div>

    <script>
        function validateForm() {
            const name = document.getElementById('name').value.trim();
            const nameError = document.getElementById('name-error');
            if (!name) {
                nameError.style.display = 'block';
                return false;
            } else {
                nameError.style.display = 'none';
            }
            return true;
        }
    </script>
</body>

</html>
